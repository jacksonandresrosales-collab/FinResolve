package com.finresolve.repository;
import com.finresolve.model.VwReclamosResumen;
import org.springframework.data.jpa.repository.JpaRepository;
public interface VwReclamosResumenRepository extends JpaRepository<VwReclamosResumen, Long> {
}